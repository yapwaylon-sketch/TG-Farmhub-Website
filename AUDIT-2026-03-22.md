# TG FarmHub — Full Website Audit (2026-03-22)

## Overall Status: GOOD — 12 issues found, 0 critical

---

## Issues to Fix (Priority Order)

### HIGH PRIORITY

#### 1. "Ladang PND" still in spraytracker.html print headers
- **Line 82**: `<h2>Ladang PND — TG FarmHub</h2>` (summary print header)
- **Line 247**: `<h2>Ladang PND — TG FarmHub</h2>` (reports print header)
- **Fix**: Change to "TG Agro Fruits Sdn Bhd"

#### 2. Base64 logo still in index.html and inventory.html
- **index.html line 21**: Login logo uses inline base64 data
- **inventory.html line 24**: Login/loading logo uses inline base64 data
- **Fix**: Replace with `<img src="assets/logo.png?v=2" alt="TG Agro Fruits">`

#### 3. sales-trial.html has old company names (3 places)
- Line 153: "Ladang PND — TG FarmHub"
- Line 3874: "Ladang PND — TG FarmHub"
- Line 4406: "TG Group Sdn Bhd | Ladang PND"
- **Fix**: Update to match sales.html, or delete if no longer needed

### MEDIUM PRIORITY

#### 4. Missing desktop font breakpoints in 4 module CSS files
- `inventory.css`, `workers.css`, `spraytracker.css`, `growthtracker.css` — none have `@media (min-width: 1024px)` breakpoints
- `shared.css` and `sales.css` have them — other modules will have smaller text on desktop
- **Fix**: Add desktop breakpoint to each module CSS

#### 5. trapFocus memory leak in shared.js
- **Line 306-308**: Escape key handler hides modal but never calls `releaseFocus()`
- **Line 264**: confirmAction() uses trapFocus but `modal.remove()` leaves dangling handler
- `releaseFocus()` (line 320) exists but is never called anywhere
- **Impact**: Minor memory leak if many modals opened/closed
- **Fix**: Call `releaseFocus()` before hiding/removing modals

#### 6. Duplicate CSS rules in sales.css
- `.order-item-row` defined twice (lines ~471 and ~894)
- `.so-detail-header` has duplicate margin-bottom
- **Fix**: Remove duplicates

#### 7. Z-index inconsistency
- `.cal-popup` (z-index: 9999) same as `.notification-container` (9999) — could overlap
- `.bottom-actions` (z-index: 100) could hide under dynamically created elements
- **Fix**: Standardize z-index scale

### LOW PRIORITY

#### 8. Sidebar logo size inconsistency on mobile
- shared.css: 84px → 40px at 768px
- inventory.css: overrides to 28px at 768px
- **Fix**: Standardize mobile logo size

#### 9. dbNextId fallback collision risk (shared.js line 335)
- If DB is down, uses `Date.now().slice(-6)` — rapid calls get same ID
- **Impact**: Very low (DB rarely down, and this is fallback only)
- **Fix**: Add random suffix to fallback

#### 10. Missing mobile breakpoints in spraytracker.css and growthtracker.css
- No `@media (max-width: 768px)` rules for filter bars and report buttons
- **Fix**: Add mobile-specific sizing

#### 11. Duplicate button styles across module CSS files
- Report buttons (`.report-btn`) duplicated in spraytracker.css and sales.css
- Filter/tab buttons similar across spraytracker, growthtracker, sales
- **Fix**: Consolidate into shared.css (optional cleanup)

#### 12. Minor div count mismatch in sales.html
- 490 opening divs vs 485 closing divs (5 extra opens)
- **Impact**: No functional issues observed — likely from dynamically generated modals
- **Fix**: Verify and correct if any specific rendering issue found

---

## Passing Checks (All Clean)

| Check | Status |
|-------|--------|
| Modal click-outside-to-close removed | PASS — 0 instances across all files |
| .select() on all Supabase mutations | PASS — verified 90+ calls across all modules |
| Logo cache busting (?v=2) | PASS — all external refs have ?v=2 |
| Status labels (Order Received, Ready For Delivery) | PASS — correctly renamed |
| Sidebar structure (integrated brand+back link) | PASS — all 5 modules updated |
| Mobile logout hidden in sidebar | PASS — shared.css handles it |
| Session/auth guard | PASS — 30min timeout, proper cleanup |
| Offline handling | PASS — banner, retry, onLine checks |
| sbQuery/sbMutate error handling | PASS — excellent with retries |
| Company name in sales.html | PASS — "TG Agro Fruits Sdn Bhd" everywhere |
| Company name in delivery.html | PASS — updated |
| Receipt format (80mm, B&W, monospace) | PASS — correctly styled |
| Bottom action bar sidebar offset | PASS — left: var(--sidebar-w) |
| Customer dropdown close-on-outside-click | PASS — capture phase listener |

---

## Recommendations for Next Session

1. Fix the 3 high-priority items (spraytracker print headers, base64 logos, sales-trial)
2. Add desktop breakpoints to the 4 module CSS files
3. Fix trapFocus memory leak in shared.js
4. Consider the light theme visual overhaul (trial-theme.css ready)
