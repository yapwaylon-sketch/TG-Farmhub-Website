# Local Netlify folder
.netlify

# Claude Code local settings — per-PC state stays ignored,
# but project-scoped skills/agents DO sync via git so both PCs share them.
.claude/*
!.claude/skills/
!.claude/agents/

# Sub-projects (separate Netlify deploys, same Supabase DB)
# Nanas is now its own repo at yapwaylon-sketch/TG-Nanas-Growth-TV,
# cloned as a sibling at C:\dev\TG-Nanas-Growth-TV (no longer a subfolder).
# Weather is still deferred — lives outside the project folder entirely.
TG Weather Monitoring Website/

# Node / tooling
node_modules/

# Supabase CLI local cache (project-ref, pooler-url, version pins)
supabase/.temp/

# Superpowers / deep-plan scratch output
.superpowers/
docs/superpowers/specs/sections/.prompts/

# Icon design scratch (icons/modules/ is committed, icons/test/ is not)
icons/test/
